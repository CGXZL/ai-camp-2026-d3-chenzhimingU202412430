# D3 答辩准备（3 分钟）

> 每页只讲一个中心意思；数字旁能说清数据划分、指标或命令；答辩者要能脱离智能体逐步解释代码和证据。

## 逐页讲稿（8 页 · 全程 3 分钟）

### 第 1 页 · 封面 · 约 5 秒

大家好，这是 D3：预测下一小时家庭用电。

### 第 2 页 · 问题与使用者 · 约 20 秒

家庭能源研究小组想提前看到用电变化，但不能依赖昂贵系统。任务：用一份真实单户分钟用电记录，比较“下一小时 ≈ 上一小时”的持续性基线与固定随机森林候选，预测下一小时平均功率。边界一句话：单户数据不能代表其他家庭，也不能用于电网控制或安全告警。

### 第 3 页 · 真实数据 · 约 20 秒

数据来自 Kaggle 指定页面：2,075,259 条分钟记录，`python analyze.py --check-data` 通过（rows 2075259、9 列）。我们取前 150,000 分钟聚合为 2,501 小时平均功率（2006-12 至 2007-03）。

### 第 4 页 · 方法与简单基线 · 约 25 秒

特征只用预测时刻以前的 `lag_1/2/3/24` 与 `hour_of_day`，不引入未来信息。按时间先后前 80% 训练、后 20% 测试（496 小时），不随机打乱。基线用最近一小时直接预测下一小时——零学习、可解释。

### 第 5 页 · 候选方法 · 约 20 秒

候选只在原流程后加一个固定种子随机森林（`RandomForestRegressor(n_estimators=100, random_state=42)`），其余数据、划分、指标全不变，所以比较公平。

### 第 6 页 · 最重要证据：主指标 · 约 30 秒

同一 496 小时测试区间：基线 MAE 0.794 kW、候选 0.573 kW；高需求误报从 31 降到 9。但两模型高需求召回率都只有 0.184（38 个高需求小时只认出 7 个）——平均指标变好，不代表更会“报尖峰”。

### 第 7 页 · 最重要证据：真实失败案例 · 约 30 秒

`largest_errors.csv` 第一行：2007-03-28 17:00 真实 0.386 kW、预测 3.606 kW，误差 3.22 kW——突然负荷骤降，滞后特征无法预见。另一方向案例 2007-03-11 12:00 骤升低估（真实 3.31 / 预测 0.77）。

### 第 8 页 · 失败、限制与下一步 · 约 30 秒

滞后模型对突增/突降严重失准；证据不能证明该家庭“一定出门了”或结果适用于其他家庭。下一项最小检查：加入 `lag_24` 或功率变化率特征，看最大误差是否下降。结论：候选平均误差优于基线，但高需求告警两者同样不足。

---

## 教师追问准备

### 1. 从一条真实输入追踪到输出

以 `2007-03-28 17:00:00`（真实 0.386 kW、预测 3.606 kW）为例：

1. `analyze.py --check-data` 验证 `data/raw/household_power_consumption.txt`（rows: 2075259）；
2. `--prepare` 读前 150000 条分钟记录，按小时求平均 → `data/processed/hourly_power.csv`（2501 小时）；
3. `make_lagged` 按时间排序，生成 `lag_1/2/3/24`、`hour_of_day` 和 `target_next`（`shift(-1)`），丢弃缺失行；
4. `chronological_split` 前 1980 小时训练、后 496 小时测试；
5. `build_candidate` 固定种子随机森林 `.fit(train)`、`.predict(test)`；
6. `forecast_metrics` 算 MAE/RMSE；`largest_errors.csv` 保存误差最大的 12 行；
7. 这一小时真实功率接近零而预测 3.606，因此排在最大误差第一行。

### 2. 为什么这个基线足够简单

一个规则、不学习、可解释——“下一小时和上一小时差不多”。它是判断“随机森林是否值得额外复杂度”的最低参照；只有明显更好，才值得承担它的不透明性。

### 3. 一个指标怎样计算、为什么与使用者有关

**MAE** = 每个测试小时 `|真实功率 − 预测功率|` 的平均 = 0.573 kW。它直接回答“平均相差多少 kW”，对使用者最直观。但平均会掩盖少数巨大错误，所以要再看**最大误差时刻**和**高需求召回率**：召回率 = 真阳性 ÷ (真阳性 + 假阴性) = 7 ÷ 38 ≈ 0.184，回答“真正的高需求小时有多少被提示”，这正是研究小组最关心的。

### 4. 一个失败案例和下一项检查

2007-03-28 17:00 骤降案例：滞后特征无法预见突变。下一项检查：加入 `lag_24` 或“功率变化率”特征，观察最大误差是否下降。

### 5. 智能体建议了什么，学生如何验证

智能体建议实现滞后特征与固定种子候选、补齐 `--check-data`。学生逐行复核代码，用 README 命令从零重跑并对比 `metrics.json`/`largest_errors.csv`，确认数字可复现，且没有随机打乱时间顺序、没有改测试或阈值。
